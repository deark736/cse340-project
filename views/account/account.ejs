<h1><%= title %></h1>

<%- messages() %>

<% if (errors) { %>
  <ul class="error">
    <% errors.array().forEach(e => { %>
      <li><%= e.msg %></li>
    <% }) %>
  </ul>
<% } %>

<p>You’re logged in</p>